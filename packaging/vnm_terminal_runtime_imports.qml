// Deployment manifest for QML imports embedded as C++ strings or resources.
// The file is scanned by Qt's deployment tooling and is not loaded at runtime.
import QtQuick
import QtQuick.Window
import QtQuick.Controls
import QtQuick.Controls.Basic
import QtQuick.Layouts

Item {}
